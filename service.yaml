apiVersion: v1
kind: Service
metadata:
  name: speech-backend-service
spec:
  selector:
    app: speech-backend
  type: NodePort
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30001
